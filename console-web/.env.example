# Default WS target for apollo-console (Vite dev + prod build).
# Override per-install by copying to .env.local and editing.
# UI host/port inputs still override this at runtime; localStorage overrides both.
VITE_WS_URL=ws://localhost:4721
